<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="dk.blackdarkness.g17.cphindustries">

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_ci_logo_small_trans"
        android:label="@string/app_name"
        android:logo="@drawable/ic_ci_logo_small_trans"
        android:roundIcon="@drawable/ic_ci_logo_small_trans"
        android:supportsRtl="true"
        android:theme="@style/CPHIndustriesTheme">
        <activity
            android:name=".activities.ViewMainActivity"
            android:label="@string/app_name"
            android:configChanges="orientation|screenSize"
            android:theme="@style/CPHIndustriesTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".menuitems.SettingsActivity"
            android:label="Settings"
            android:configChanges="orientation|screenSize"
            android:parentActivityName=".activities.ViewMainActivity">
        </activity>
        <activity
            android:name=".menuitems.AboutActivity"
            android:label="About"
            android:parentActivityName=".activities.ViewMainActivity">
        </activity>

        <meta-data
            android:name="io.fabric.ApiKey"
            android:value="3880ff607ba8029ca6386dcb8788d10d9151e29d" />

    </application>
    <uses-permission android:name="android.permission.INTERNET" />
</manifest>